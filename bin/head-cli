#!/usr/bin/env node

require('commander')
  .version(require('../package').version)
  .usage('<command> [options]')
  .command('doctor', 'help to diagnosis the problems')
  .command('serverless', 'package the serverless.zip')
  .parse(process.argv);
